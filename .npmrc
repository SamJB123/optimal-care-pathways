# Cloudflare's miniflare + workerd binaries must be unpacked, so build scripts are
# allowed for known-good packages only (pnpm-workspace.yaml -> allowBuilds).
strict-peer-dependencies=false
auto-install-peers=true
